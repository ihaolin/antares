# The server bind address
BIND_ADDR=127.0.0.1

# The server listening port
LISTEN_PORT=22111

# The redis host
REDIS_HOST=127.0.0.1

# The redis port
REDIS_PORT=6379

# The redis namespace
REDIS_NAMESPACE=ats

# The log path
LOG_PATH=./logs

# The zookeeper hosts
ZK_SERVERS=127.0.0.1:2181

# The zookeeper namespace
ZK_NAMESPACE=ats

# Java Heap options
JAVA_HEAP_OPTS="-Xms512m -Xmx512m -XX:MaxNewSize=256m"

# The tower user
TOWER_USER=admin

# The tower passowrd
TOWER_PASS=admin


### The alarm configuration
ALARM_ENABLE=false

## You don't need to config bellow items if ALARM_ENABLE=false

# The alarm notify type
# 1：EMAIL
#
ALARM_NOTIFY_TYPE=1

# The alarm subject
ALARM_SUBJECT=Antares任务报警

# The alarm job timeout template
ALARM_TEMPLATE_JOB_TIMEOUT=应用【{appName}】的任务【{jobClass}】执行超时，调度服务器为【{scheduler}】：{detail}.

# The alarm job failed template
ALARM_TEMPLATE_JOB_FAILED=应用【{appName}】的任务【{jobClass}】执行失败，调度服务器为【{scheduler}】：{detail}.

### The email configuration

# The mail host
MAIL_HOST=192.168.0.1

# The mail from address
MAIL_FROM_ADDR=haolin.h0@gmail.com

# The mail from username
MAIL_FROM_USER=haolin.h0

# The mail from password
MAIL_FROM_PASS=123456

# The mail to receivers
MAIL_TO=haolin.h0@gmail.com